# User Preferences
- Communicates informally with typos and casual tone — does not spell-check messages. Expects the agent to understand intent without asking for clarification on typos. Confidence: 0.85
- Provides regression context when reporting bugs ("it was working a while ago") — expects the agent to investigate what changed, not just the current state. Confidence: 0.8
- Expects the agent to read official documentation when debugging (provides doc URLs explicitly). Confidence: 0.9
- Wants end-to-end verification of fixes — not just code changes but confirmation that the feature actually works in the live environment ("insure when the sandbox goes live and browser session is attached, insure the profile is added and is working"). Best received: drive the exact consumer path before reporting success — e.g. spawn the fixed MCP server in a sandbox and drive it over stdio JSON-RPC exactly like opencode does, with a hard verdict check ("LOGGED IN ✓" vs sign-in wall) — rather than claiming fixed from code inspection alone. Confidence: 0.9
- Prefers thorough root cause analysis over surface-level patches. When the same problem recurs ("I am getting this problem again and again and again"), expects a durable fix that eliminates the failure mode entirely (e.g. fail-fast pre-flight validation), not another patch. Confidence: 0.85
- Works on a browser automation platform (Doppel) using Solari SDK with Next.js API routes and harness/snapshot architecture. Confidence: 0.95
- Uses WSL on Windows (project at `/mnt/c/Users/ASUS/Desktop/doppel/`). Builds run on Windows (PowerShell), not WSL — avoid installing platform-specific native packages (e.g. `*-linux-*`) from WSL as they break Windows `npm install`. Confidence: 0.95
- Uses opencode as the LLM executor inside Solari sandboxes. Confidence: 0.9
- Prefers the agent to skip lengthy analysis/explanations and just write code directly — "just write code stop thinking". Confidence: 0.9
- Expects debugging hypotheses to be validated empirically against the live system before accepting any explanation — real API calls with their credentials, controlled A/B tests (e.g. `autoLogin: true` vs `false` session creation), and inspecting bundled third-party package internals (npm pack + grep the dist bundle) for undocumented field names and defaults; responds well to "verified with live API calls, 100% certain" reporting. Confidence: 0.8
- Makes strong but sometimes incorrect causal assertions ("I am sure the browser is not using the profile at all") — test these directly rather than accepting or dismissing them; accepts blunt, evidence-backed corrections (e.g. proved the cookies were fine and the model's own actions caused the logout). Confidence: 0.75
- After every code edit, run a verification before reporting the fix as done — but a plain TypeScript transpile check is NOT enough: it never resolves module specifiers, so a wrong relative import depth (`../../../convex/_generated/api` instead of `../../../../`) shipped clean and was only caught by the user's own `npm run build` (Next.js/Turbopack). Verify imports actually resolve (count `../` against directory nesting, tsc --noEmit, or the real build) — the user's acceptance bar is a passing production build, and anything that passes transpile but fails `npm run build` counts as a miss. Confidence: 0.8
- When probing metered external services (Solari sessions), release/clean up test resources immediately after verifying to avoid cost. Confidence: 0.6
- Doppel harness runs a weak free LLM (mimo-v2.5-free) that sabotages its own sessions by misusing human-handoff tools (`solari_browser_login` revokes browser access) and clicking "Sign in" — harness prompts must harden it: explicit NEVER-lists for dangerous tools, mandated exact parameters (`profileId` + `autoLogin: false`), and an immediate `needsAuth` exit on login walls. Confidence: 0.8
- When the local WSL environment cannot reach a remote endpoint (e.g. CDP connect timeouts), reproduce the test inside the exact target environment — spin up a Solari sandbox from the project's snapshot and run the script there — instead of approximating locally. Confidence: 0.75
- Keep API keys out of command output and logs: redact secrets when grepping/echoing `.env.local` (pipe through `sed` to `***`), and inject credentials into test environments via files (e.g. `/tmp/env.json`) rather than echoing them. Confidence: 0.8
- Windows build-lock gotcha: a running dev/prod server locks `.next` files, so rebuilds die with EPERM unlinking (e.g. `.next/node_modules/@solarisdk/browser-…`) while the user only sees "build worker exited with code: 1" and `npm start` failing on a missing `prerender-manifest.json`. Fix: stop the server, wipe `.next` (`cmd.exe /c "rmdir /s /q .next"`), rebuild. When the user pastes a cryptic build-worker exit, reproduce the real build to surface the hidden error instead of guessing. Confidence: 0.75
- Verification tests: write the script to a scratchpad file (not a fragile inline `node -e` one-liner) with timestamped progress logs, but KEEP TIMEOUTS SMALL and fail fast — explicitly demanded "don't make me wait, keep the timeout small" when left waiting on a long background test. Prefer quick foreground runs with hard internal caps (Promise.race + process.exit, e.g. 25s dial timeout, 90–110s total) over long background waits; kill hung tasks immediately on user report; confirm live external state (e.g. the session actually exists via a quick API call) before telling the user a test is running. Confidence: 0.9
- Ad-hoc scripts importing project packages (`@solarisdk/*`) must live inside the project directory for `node_modules` resolution — copy the runner into the project (e.g. `.commandcode-sandbox-test.mjs`) and run it from there. Confidence: 0.7
- Solari platform gotcha (proven by live test): the gateway accepts `profileId` and reports storageState cookies "attached" (60+) but NEVER injects them into the browser context (0 cookies in ctx at t+0–9s, with and without autoLogin), and `@solarisdk/mcp` never applies them client-side — stock profile-based login is broken. Fix lives in the harness: a corrected browser MCP server (`/tmp/solari-browser-mcp.mjs`, written into the sandbox by `app/api/run/route.ts` on every run) that applies `session.storageState` cookies + localStorage client-side via patchright and returns `cookiesApplied` in the create response. Confidence: 0.9
- Dial Solari sessions via `session.wsEndpoint` (the SDK's LocalProxy-wrapped endpoint), not the raw `session.cdpEndpoint` — the raw CDP endpoint times out even when connecting from inside a Solari sandbox. Confidence: 0.85
- Judges product quality by the observable end-user experience, not just functional correctness — confirmed a run's real-world success ("I can confirm I received the email") while still demanding fixes because "the live trace seems bad, the final response is really bad, overall feel is bad even though the task was completed." Treats wrong status reporting (successful runs marked "failed"), raw JSON dumps in the trace, and unreadable final responses as bugs worth fixing proactively; wants humanized trace events and clean completion cards instead. Confidence: 0.9
- Grants broad autonomy for product improvement — when asking to improve the app, explicitly delegates latitude to change how it actually works and add must-have features ("Have some freedom and apply changes, not just to UI, UX, but the actual app, how it works, how to improve, any must have feature? go on"), preferring the agent to take substantive initiative rather than making only cosmetic changes or asking which improvements to make. Confidence: 0.85
- Treats a missing kill switch as a must-have product gap: any long-running operation (active sandbox session, running browser task) needs a visible Stop control in the UI plus an authenticated, idempotent kill API ("why is there no kill switch for the current session and sandbox?"). Killing should mark the session stopped with a human reason ("Stopped by you") and treat already-dead sandboxes (404) as success. Confidence: 0.9
- Treats branding as a first-class product requirement, not an afterthought — explicitly requested favicon + Open Graph work ("work on favicon and Open Graph, branding matters"). Expect the brand surface (favicon, OG/Twitter cards, metadata) to be shipped proactively with any web app work rather than waiting to be asked. Confidence: 0.9
- End-user flows must be fully self-serve inside the app — never depend on an external admin console or the developer's own account. User flagged that browser-profile login only worked via the Solari console ("I used solari console but how is my user going to do the same?"). First fix (in-app Settings "Log in to X" button wired to Solari's login-handoff API, live save-detection by polling the profile-version bump) turned out INSUFFICIENT: the handoff URL is actually the auth-gated console editor (`console.getsolari.com/profiles/{id}/edit`) and 307-redirects anonymous visitors to Solari's own login wall — the user caught it by clicking the button ("my solari console with my account logged in is opened. Is that how its supposed to work? No I think"). Proper fix (proposed, not yet built): host the login viewer inside Doppel — open a cloud browser session on the site's real login page, stream screenshots + click/type/key overlay into Settings, capture storageState on success, credentials never touch Doppel. Verification lesson: before claiming an external-URL flow works for end users, fetch that URL AS AN ANONYMOUS VISITOR (status code + redirect target) — the developer's own logged-in session masks auth gates, and the user dogfoods flows and reliably catches these gaps. Generalizes: any UI copy, error hint, or docs step that tells end users to open an external console or dev-only URL is a bug — replace it with actionable in-app steps across every surface that mentions it (pre-flight errors, harness prompts, explainers), not just the primary button. Confidence: 0.9
- Doppel constraint to remember: browser profiles are account-scoped under the single `SOLARI_API_KEY` — all end users share one Solari org. Deliberately accepted pre-billing; revisit per-user orgs or a proxying layer when users are billed. Confidence: 0.7
- User explicitly re-prioritizes the agenda mid-session ("ok clerk can wait a little. first things first") — drop the deprioritized thread without argument and take up the newly named priority immediately. Confidence: 0.6
- React Rules of Hooks gotcha (user hit it live in `app/settings/page.tsx`): in components with early returns (`if (!isAuthenticated) return …`), every new hook must be registered BEFORE the first early return — a hook placed after a conditional return changes the hook count between renders and throws "React has detected a change in the order of Hooks". When editing hook-heavy components, insert new useState/useEffect alongside the other hooks up top, then verify with grep that the last `use(State|Effect|Memo)(` line number precedes the first early-return line number. A TypeScript transpile check cannot catch this class of bug — only the runtime/React dev overlay reports it. Confidence: 0.8
- Reports bugs by pasting the complete verbatim error output — full console errors with hook-order tables, stack traces, code frames (exact file:line), and framework versions ("Next.js version: 16.3.4 (Turbopack)") — with no prose around it. Jump straight to the embedded file/line and fix the root cause without asking for clarification. Confidence: 0.7
- When preparing external writeups (e.g. bug reports for Solari's founder/engineers), explicitly demands claims be verified with proof BEFORE drafting ("verify it with proof and prepare a writeup") — expects a detailed, engineer-ready document built from fresh live evidence: UTC-timestamped capture runs, a deterministic copy-pasteable repro script plus its actual observed output, environment/SDK versions, per-claim proof tables, concrete suggested fixes, and a "what works well" section so the tone stays collaborative rather than accusatory. Confidence: 0.85
